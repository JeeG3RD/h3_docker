# TP 3 - FastAPI Introduction

## Installation de FastAPI  

``` pip install fastapi ```  
``` pip install uvicorn ```

## Démarrer l'application

``` uvicorn file:app-name --reload```

### Ou insérer à la fin du script python

``` uvicorn.run(app-name,host="127.0.0.1",port="8000") ```

